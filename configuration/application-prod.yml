spring:
  application:
    name: delivery-system
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
delivery-system:
  value: "VALOR - PROD"
  default-value: "VALOR DEFAULT - PROD"
